package com.smartprocurement.entity;

public enum BookingStatus {
    BOOKED,
    CONFIRMED,
    WAITING,
    CALLED,
    ARRIVED,
    PROCESSING,
    COMPLETED,
    SKIPPED,
    CANCEL_REQUESTED,
    CANCELLED,
    RESCHEDULE_REQUESTED,
    RESCHEDULED,
    REJECTED
}
