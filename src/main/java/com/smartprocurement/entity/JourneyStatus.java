package com.smartprocurement.entity;

public enum JourneyStatus {
    SCHEDULED,
    CALLED,
    TRAVELLING,
    ARRIVED,
    COMPLETED,
    MISSED_ARRIVAL,
    UNREACHABLE,
    CANCEL_REQUESTED,
    RESCHEDULE_REQUESTED
}
